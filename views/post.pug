extends layout

block content
  main.single
    .intro
      h1.title= post.title
      .meta
        .date=post.createdStr
        .tags Tags: 
          - var i = 0
          each tag in post.tags
            - var str = ', '
            if (i == post.tags.length - 1)
              - str = ''
            a(href='/tag/' + tag) #{tag}
            =str
            - i++ 
        .desc= post.desc
    article!= post.content
